# Database Configuration
DB_PORT=5432#Change this if your database is running on a different port
DB_NAME=field_db#Change this to your desired database name
DB_USER=admin#Change this to your desired database username
DB_PASSWORD=password123#Change this to your desired database password
DB_HOST=postgres#Change this if your database is running on a different host

# Optional: Backend Configuration
# SPRING_PORT=8080

# Optional: Frontend Configuration
# REACT_APP_API_URL=http://localhost:8080/api
